---
description: 用户结束工作时自动提交代码并推送到 GitHub
alwaysApply: true
---

# 结束工作：自动保存并推送 GitHub

## 触发时机

当用户表示**结束工作、下班、先这样、保存一下、推送 GitHub**等类似意思时，在回复前先完成 Git 保存与推送。

## 仓库信息

- 本地路径：`d:\waimai\new-alien-app`
- 远程：`origin` → `https://github.com/wanger2651-cloud/new-alien-app.git`
- 默认分支：`main`

## 执行步骤（必须按顺序）

1. 并行检查：`git status`、`git diff`、`git log -3 --oneline`
2. 确认**不提交**：`.env`、密钥/证书、`node_modules/`、`dist/`、`unpackage/`、`*.log`、`hilog-dump.txt`、`runtime-hilog*.txt`
3. 若有未提交改动：
   - `git add` 相关源码与文档（`src/`、`docs/`、`scripts/`、配置文件等）
   - 用 HEREDOC 写 1–2 句中文 commit message，说明「为什么」
   - `git commit`
   - `git push -u origin HEAD`（或 `git push origin main`）
4. commit 失败（hook 拒绝）→ 修问题后**新建 commit**，不要 amend（除非用户明确要求）
5. push 失败 → 报告原因，**不要** force push `main`
6. 最后 `git status` 确认干净或说明剩余未提交项

## 同步说明

- 微信小程序改动在 `src/`，编译产物在 `dist/`（已在 .gitignore，不提交）
- 若用户同时维护 `D:\qingning\new-alien-app`，仅在用户要求时再双向同步

## 回复用户

推送完成后简要说明：提交了什么、GitHub 分支、有无未推送/未提交文件。
